QSP-Game Проверяет, существует ли ячейка. Т.е. фактически проверяет существуют ли строка и колонка

!@ $0 - line
!@ $1 - column
!@ $2 - table
# edb.cell.is
result = iif(@edb.row.is($args[0], $args[2]) and arrsize('$<<$args[2]>>_<<$args[1]>>') > 0, 1, 0)
--- edb.cell.is ---------------------------------

Не переключает указатели текущих столбца, строки и таблицы
