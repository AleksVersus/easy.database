QSP-Game проверка существования строки в таблице

$args[0] - идентификатор таблицы данных
$args[1] - идентификатор строки. Первичный ключ.

# edb.dt.is_row
if $arritem('$<<$args[0]>>', 'ID') = '':
	msg 'dt.is_row: Таблица данных «<<$args[0]>>» не создана.'
	exit
end
result = iif(arrpos('$<<$args[0]>>_id', $args[1]) <> -1, 1, 0)
--- edb.dt.is_row ---------------------------------

Это не метод, это вспомогательная функция
